#include <iostream>

using namespace std;

int main(void) {
    int a,b;
    cin >> a >> b;
    // cout << a << " + " << b << " = " << (a + b) << endl;
    printf("%d + %d = %d\n", a,b,a+b);
    return 0;
}